# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'VideoProcessing' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  
  pod ‘DKImagePickerController’, ‘~> 3.4.2’
  

  # Pods for VideoProcessing

  target 'VideoProcessingTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VideoProcessingUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
